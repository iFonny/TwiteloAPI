const fieldSettings = {
    account: {
        type: 'account', // select
        tooltip: false,
        input: false, // already handled by the component
        label: { // required
            en: 'Account',
            fr: 'Compte'
        }
    }
};

module.exports = {};